apiVersion: v1
kind: Service
metadata:
  name: catalog-java
  labels:
    app.kubernetes.io/part-of: promptlab
    app.kubernetes.io/managed-by: kubectl
spec:
  selector:
    app: catalog-java
  ports:
    - name: http
      port: 8080
      targetPort: http
