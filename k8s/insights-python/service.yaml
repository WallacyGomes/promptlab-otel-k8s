apiVersion: v1
kind: Service
metadata:
  name: insights-python
  labels:
    app.kubernetes.io/part-of: promptlab
    app.kubernetes.io/managed-by: kubectl
spec:
  selector:
    app: insights-python
  ports:
    - name: http
      port: 8000
      targetPort: http
