apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: promptlab
resources:
  - service.yaml
  - statefulset.yaml
configMapGenerator:
  - name: postgres-init
    files:
      - 001-init.sql
